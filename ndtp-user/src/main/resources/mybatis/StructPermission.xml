<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ndtp.persistence.StructPermissionMapper">

	<!-- 세션 정보 -->
	<select id="selectStructPermission" resultType="structPermission">
		<![CDATA[
			select perm_seq, constructor, constructor_type, perm_officer, birthday, license_num, phone_number, is_complete, latitude, longitude, apply_date
			from struct_permission
		]]>
	</select>
	<select id="selectOne" parameterType="structPermission" resultType="structPermission">
		select * from struct_permission where constructor = #{constructor}
	</select>

	<insert id="insertStructPermission" parameterType="structPermission">
		<![CDATA[
			INSERT INTO struct_permission (
				perm_seq, constructor, constructor_type, perm_officer, birthday, license_num, phone_number, is_complete, latitude, longitude, apply_date
			) VALUES (
				'0', '${constructor}', '${constructor_type}', '${perm_officer}', '${birthday}', '${license_num}', '${phone_number}', '${is_complete}', '${latitude}', '${longitude}', now()
			)
		]]>
	</insert>

	<update id="updateStructPermission" parameterType="structPermission">
		update struct_permission set is_complete='Y' where constructor = #{constructor}
	</update>
</mapper>
