<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ndtp.persistence.StructPermissionMapper">

	<!-- 세션 정보 -->
	<select id="selectStructPermission" resultType="structPermission">
		select * from struct_permission ;
	</select>

	<insert id="insertStructPermission" parameterType="structPermission">
		<![CDATA[
			INSERT INTO public.struct_permission
			(perm_seq, constructor, constructor_type, birthday, license_num, phone_number, is_complete, apply_date)
			VALUES(0, '${constructor}', '${constructor_type}', '${birthday}', '${license_num}', '${phone_number}', '${is_complete}', now())
		]]>
	</insert>
</mapper>